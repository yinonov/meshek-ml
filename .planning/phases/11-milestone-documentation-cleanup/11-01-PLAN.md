---
phase: 11-milestone-documentation-cleanup
plan: "01"
title: "Documentation Artifact Cleanup"
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-recommendation-engine/06-01-SUMMARY.md
  - .planning/phases/08-api-surface-deployment/08-01-SUMMARY.md
  - docs/deploy-cloudrun.md
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - MODEL-01
  - MODEL-02
autonomous: true
---

# Plan 01: Documentation Artifact Cleanup

## Goal

Fix all v1.1 milestone documentation debt identified in the milestone audit: unchecked requirement checkboxes, stale traceability statuses, missing SUMMARY frontmatter fields, and undocumented Python env skew.

## Tasks

<task id="1">
<title>Check REQUIREMENTS.md checkboxes for API-01..04, MODEL-01..02</title>
<read_first>
- .planning/REQUIREMENTS.md
</read_first>
<action>
In `.planning/REQUIREMENTS.md`, change these 6 lines from unchecked to checked:

```
- [ ] **API-01**: meshek app can check service health via `GET /health`
```
to
```
- [x] **API-01**: meshek app can check service health via `GET /health`
```

```
- [ ] **API-02**: meshek app can create a new merchant with zero configuration via `POST /merchants`
```
to
```
- [x] **API-02**: meshek app can create a new merchant with zero configuration via `POST /merchants`
```

```
- [ ] **API-03**: meshek app can submit daily sales records for a merchant via `POST /sales`
```
to
```
- [x] **API-03**: meshek app can submit daily sales records for a merchant via `POST /sales`
```

```
- [ ] **API-04**: meshek app can get per-product order recommendations for a merchant via `POST /recommend`
```
to
```
- [x] **API-04**: meshek app can get per-product order recommendations for a merchant via `POST /recommend`
```

```
- [ ] **MODEL-01**: Service starts in non-degraded mode ...
```
to
```
- [x] **MODEL-01**: Service starts in non-degraded mode ...
```

```
- [ ] **MODEL-02**: Model bundle is reproducibly trainable ...
```
to
```
- [x] **MODEL-02**: Model bundle is reproducibly trainable ...
```
</action>
<acceptance_criteria>
- `grep -c '\- \[x\]' .planning/REQUIREMENTS.md` returns 17 (all requirements checked)
- `grep -c '\- \[ \]' .planning/REQUIREMENTS.md` returns 0 (no unchecked v1.1 requirements)
- `grep '\[x\] \*\*API-01\*\*' .planning/REQUIREMENTS.md` matches
- `grep '\[x\] \*\*API-04\*\*' .planning/REQUIREMENTS.md` matches
- `grep '\[x\] \*\*MODEL-01\*\*' .planning/REQUIREMENTS.md` matches
- `grep '\[x\] \*\*MODEL-02\*\*' .planning/REQUIREMENTS.md` matches
</acceptance_criteria>
</task>

<task id="2">
<title>Update REQUIREMENTS.md traceability table to show all 17 as Complete</title>
<read_first>
- .planning/REQUIREMENTS.md
</read_first>
<action>
In the Traceability table of `.planning/REQUIREMENTS.md`, change these rows from `Pending` to `Complete`:

| API-01 | Phase 8, 11 | Pending | to | API-01 | Phase 8, 11 | Complete |
| API-02 | Phase 8, 11 | Pending | to | API-02 | Phase 8, 11 | Complete |
| API-03 | Phase 8, 11 | Pending | to | API-03 | Phase 8, 11 | Complete |
| API-04 | Phase 8, 11 | Pending | to | API-04 | Phase 8, 11 | Complete |
| INFRA-03 | Phase 8.1, 10 | Pending | to | INFRA-03 | Phase 8.1, 10 | Complete |
| MODEL-01 | Phase 9, 11 | Pending | to | MODEL-01 | Phase 9, 11 | Complete |
| MODEL-02 | Phase 9, 11 | Pending | to | MODEL-02 | Phase 9, 11 | Complete |

Update the coverage summary to read:
```
- Satisfied: 17 (all requirements verified complete)
```
</action>
<acceptance_criteria>
- `grep -c 'Pending' .planning/REQUIREMENTS.md` returns 0
- `grep -c 'Complete' .planning/REQUIREMENTS.md` returns 17
- `grep 'Satisfied: 17' .planning/REQUIREMENTS.md` matches
</acceptance_criteria>
</task>

<task id="3">
<title>Add requirements_completed to Phase 6 SUMMARY frontmatter</title>
<read_first>
- .planning/phases/06-recommendation-engine/06-01-SUMMARY.md
</read_first>
<action>
Add `requirements_completed` field to the YAML frontmatter of `.planning/phases/06-recommendation-engine/06-01-SUMMARY.md`. Insert after the existing frontmatter fields (before the closing `---`):

```yaml
requirements_completed:
  - REC-01
  - REC-02
  - REC-03
  - REC-04
  - INFRA-01
```

Phase 6 (Recommendation Engine) completed REC-01 through REC-04 (tiered recommendations with reasoning and confidence) and INFRA-01 (LightGBM model loads at startup via lifespan).
</action>
<acceptance_criteria>
- `grep 'requirements_completed' .planning/phases/06-recommendation-engine/06-01-SUMMARY.md` matches
- `grep 'REC-01' .planning/phases/06-recommendation-engine/06-01-SUMMARY.md` matches
- `grep 'INFRA-01' .planning/phases/06-recommendation-engine/06-01-SUMMARY.md` matches
</acceptance_criteria>
</task>

<task id="4">
<title>Add requirements_completed to Phase 8 SUMMARY frontmatter</title>
<read_first>
- .planning/phases/08-api-surface-deployment/08-01-SUMMARY.md
</read_first>
<action>
Add `requirements_completed` field to the YAML frontmatter of `.planning/phases/08-api-surface-deployment/08-01-SUMMARY.md`. Insert after the existing frontmatter fields (before the closing `---`):

```yaml
requirements_completed:
  - API-01
  - API-02
  - API-03
  - API-04
  - INFRA-02
```

Phase 8 (API Surface & Deployment) completed API-01 through API-04 (all four HTTP endpoints) and INFRA-02 (Docker container deployment).
</action>
<acceptance_criteria>
- `grep 'requirements_completed' .planning/phases/08-api-surface-deployment/08-01-SUMMARY.md` matches
- `grep 'API-01' .planning/phases/08-api-surface-deployment/08-01-SUMMARY.md` matches
- `grep 'INFRA-02' .planning/phases/08-api-surface-deployment/08-01-SUMMARY.md` matches
</acceptance_criteria>
</task>

<task id="5">
<title>Document Python env skew in deploy-cloudrun.md</title>
<read_first>
- docs/deploy-cloudrun.md
</read_first>
<action>
Add a "Known Caveats" section to `docs/deploy-cloudrun.md` (before the final `---` or at the end of the document). Content:

```markdown
## Known Caveats

### Python Version Skew: Local Training vs Cloud Run

| Environment | Python Version | Purpose |
|-------------|---------------|---------|
| Local / CI training | 3.13 | `scripts/train_model.py` generates LightGBM bundle |
| Cloud Run runtime | 3.12 | `Dockerfile` serves inference via FastAPI |

**Impact:** LightGBM model bundles are forward-compatible (trained on 3.13,
served on 3.12) because the serialised format is Python-version-agnostic
(Booster binary plus metadata with no version-specific bytecode).
No action is required unless a future dependency introduces version-specific
serialization protocols.

**Mitigation if needed:** Pin both environments to the same Python version by
updating the `Dockerfile` base image to `python:3.13-slim` when Cloud Run's
base image supports it, or pin local training to 3.12 via `pyenv local 3.12`.
```
</action>
<acceptance_criteria>
- `grep 'Python Version Skew' docs/deploy-cloudrun.md` matches
- `grep '3.13' docs/deploy-cloudrun.md` matches
- `grep '3.12' docs/deploy-cloudrun.md` matches
- `grep 'Known Caveats' docs/deploy-cloudrun.md` matches
</acceptance_criteria>
</task>

## Verification

<must_haves>
- All 17 v1.1 requirement checkboxes are checked in REQUIREMENTS.md
- Traceability table shows 17/17 Complete with zero Pending
- Phase 6 SUMMARY has requirements_completed with REC-01..04, INFRA-01
- Phase 8 SUMMARY has requirements_completed with API-01..04, INFRA-02
- Python env skew (3.13 vs 3.12) is documented in docs/deploy-cloudrun.md
</must_haves>
