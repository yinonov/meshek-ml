---
phase: 08.1-cloud-run-deployment
plan: "05"
subsystem: infra
tags: [cloud-run, gcp, gcs-fuse, sqlite, runbook, documentation]

# Dependency graph
requires:
  - phase: 08.1-cloud-run-deployment/08.1-03
    provides: scripts/bootstrap-cloudrun.sh and scripts/deploy-cloudrun.sh
  - phase: 08.1-cloud-run-deployment/08.1-04
    provides: tests/deploy/test_cloudrun_smoke.py

provides:
  - Operator runbook at docs/deploy-cloudrun.md covering bootstrap, deploy, rollback, smoke test, logs, cost, known limitations
  - Copy-pasteable gcloud commands for every operational scenario
  - Cross-link to SEED-001 for Cloud SQL migration path from Known Limitations

affects: [future-operators, SEED-001-cloud-sql-migration, 08.1-validation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "docs/ directory established for operator runbooks"
    - "Seven-section runbook structure: prerequisites → bootstrap → deploy → smoke → rollback → logs → cost → limitations"

key-files:
  created:
    - docs/deploy-cloudrun.md

key-decisions:
  - "Used two-terminal gcloud proxy pattern for smoke test (not public ingress) — explicit in runbook to prevent operators from accidentally exposing service"
  - "SEED-001 linked from Known Limitations with trigger conditions spelled out so operators know exactly when to act"
  - "DRY_RUN=1 mode documented prominently in both bootstrap and deploy sections for safe pre-flight review"

patterns-established:
  - "Runbook cross-links scripts as source of truth for flags — doc explains when/why, scripts own the how"

requirements-completed: [INFRA-03]

# Metrics
duration: 8min
completed: 2026-04-15
---

# Phase 8.1 Plan 05: Operator Runbook Summary

**Copy-pasteable Cloud Run runbook covering bootstrap, routine deploy with dry-run, IAM-proxy smoke test, traffic-shift rollback, log tailing, scale-to-zero cost model, and GCS FUSE / SQLite limitations with SEED-001 migration cross-link**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-15T12:00:00Z
- **Completed:** 2026-04-15T12:08:57Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Created `docs/deploy-cloudrun.md` — 315-line operator runbook covering all seven required sections
- Smoke test section documents the two-terminal `gcloud run services proxy` flow (explains *why* proxy is needed — internal ingress blocks direct `*.run.app` access)
- Rollback section includes both `--to-revisions=PREVIOUS=100` shorthand and the specific-revision path via `gcloud run revisions list`
- Cost table quantifies free-tier coverage at 50 merchants (compute, requests, GCS, Cloud Build all within free tier)
- Known Limitations section calls out SQLite DELETE journal mode rationale (GCS FUSE POSIX incompatibility) and links to SEED-001 with exact trigger conditions
- T-8.1-05-01 threat mitigation confirmed: smoke section exclusively uses `gcloud run services proxy` over operator IAM; no `--allow-unauthenticated` path documented

## Task Commits

1. **Task 8.1-05-01: Write docs/deploy-cloudrun.md operator runbook** — `0cdbd57` (docs)

**Plan metadata:** (included in task commit — pure-doc plan, single commit)

## Files Created/Modified

- `docs/deploy-cloudrun.md` — Operator runbook for Cloud Run deploy, rollback, logs, cost, and known limitations

## Decisions Made

- Documented `DRY_RUN=1` mode in both bootstrap and deploy sections (not just in script headers) because operators reading the doc may not inspect the script first
- Included a cost table rather than prose estimates — easier to scan and verify against GCP pricing page
- Explicit "Fly.io is preserved" limitation entry so operators do not delete `fly.toml` assuming Cloud Run supersedes it entirely

## Deviations from Plan

None — plan executed exactly as written.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. This plan creates documentation only.

## Known Stubs

None — documentation-only plan with no data sources or UI components.

## Issues Encountered

None. `docs/` directory did not exist and was created as part of task execution (expected for a first doc).

## Next Phase Readiness

- Phase 8.1 is complete: Dockerfile CMD fix (01), SQLite PRAGMA fix (02), bootstrap + deploy scripts (03), smoke test (04), operator runbook (05)
- `docs/deploy-cloudrun.md` is the interface between "code exists" and "code is operated" — Phase 8.1 is production-operable
- SEED-001 trigger conditions documented; operator knows when to open the Cloud SQL migration milestone

---
*Phase: 08.1-cloud-run-deployment*
*Completed: 2026-04-15*
