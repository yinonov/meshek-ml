---
phase: 8.1
slug: cloud-run-deployment
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-15
---

# Phase 8.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x + bash shellcheck |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Quick run command** | `.venv/bin/python -m pytest tests/storage tests/service -x -q` |
| **Full suite command** | `.venv/bin/python -m pytest tests -q` |
| **Deploy smoke command** | `MESHEK_CLOUDRUN_SMOKE=1 MESHEK_CLOUDRUN_URL=<live> .venv/bin/python -m pytest tests/deploy -x` |
| **Shell lint command** | `shellcheck scripts/deploy-cloudrun.sh scripts/bootstrap-cloudrun.sh` |
| **Estimated runtime** | ~10s (unit) + 5-15min (deploy smoke, manual) |

---

## Sampling Rate

- **After every task commit:** Run the quick command
- **After every plan wave:** Run the full suite
- **Before `/gsd-verify-work`:** Full suite green + at least one local docker rebuild smoke pass
- **Max feedback latency:** 15 seconds for unit tests

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------------|-----------|-------------------|-------------|--------|
| 8.1-01-01 | 01 | 0 | — | test scaffolding | infra | `.venv/bin/python -m pytest tests/storage tests/deploy --collect-only` | ❌ W0 | ⬜ pending |
| 8.1-01-02 | 01 | 1 | INFRA-03 | SQLite journal_mode=DELETE forced | unit | `.venv/bin/python -m pytest tests/storage/test_journal_mode.py -x` | ❌ W0 | ⬜ pending |
| 8.1-01-03 | 01 | 1 | INFRA-03 | synchronous=FULL forced | unit | `.venv/bin/python -m pytest tests/storage/test_journal_mode.py::test_synchronous_full -x` | ❌ W0 | ⬜ pending |
| 8.1-01-04 | 01 | 1 | — | existing storage tests unaffected | unit | `.venv/bin/python -m pytest tests/storage -x -q` | ✅ | ⬜ pending |
| 8.1-02-01 | 02 | 2 | INFRA-03 | Dockerfile CMD honors $PORT, defaults to 8000 | unit | `grep -E 'PORT:-8000' Dockerfile` | ✅ | ⬜ pending |
| 8.1-02-02 | 02 | 2 | INFRA-03 | Docker smoke test still passes on port 8000 | integration | `MESHEK_DOCKER_SMOKE=1 .venv/bin/python -m pytest tests/service/test_docker_smoke.py -x` | ✅ | ⬜ pending |
| 8.1-02-03 | 02 | 2 | — | JSONFormatter emits Cloud Logging severity | unit | `.venv/bin/python -m pytest tests/service/test_errors.py -x` | ✅ | ⬜ pending |
| 8.1-03-01 | 03 | 3 | INFRA-03 | `bootstrap-cloudrun.sh` enables APIs + creates bucket idempotently | infra | `shellcheck scripts/bootstrap-cloudrun.sh` | ❌ W0 | ⬜ pending |
| 8.1-03-02 | 03 | 3 | INFRA-03 | `deploy-cloudrun.sh` passes all required gcloud flags | infra | `shellcheck scripts/deploy-cloudrun.sh && bash -n scripts/deploy-cloudrun.sh` | ❌ W0 | ⬜ pending |
| 8.1-03-03 | 03 | 3 | INFRA-03 | Deploy script dry-run prints the exact `gcloud run deploy` it would run | infra | `DRY_RUN=1 bash scripts/deploy-cloudrun.sh \| grep -E 'gcloud run deploy.*--execution-environment=gen2'` | ❌ W0 | ⬜ pending |
| 8.1-04-01 | 04 | 4 | INFRA-03 | `tests/deploy/test_cloudrun_smoke.py` collects without errors in normal runs | unit | `.venv/bin/python -m pytest tests/deploy/test_cloudrun_smoke.py --collect-only` | ❌ W0 | ⬜ pending |
| 8.1-04-02 | 04 | 4 | INFRA-03 | Smoke test posts /merchants, /sales (Hebrew), /recommend against MESHEK_CLOUDRUN_URL and asserts 201/200/200 | integration | `MESHEK_CLOUDRUN_SMOKE=1 MESHEK_CLOUDRUN_URL=<live> .venv/bin/python -m pytest tests/deploy/test_cloudrun_smoke.py -x` | ❌ W0 | ⬜ pending |
| 8.1-05-01 | 05 | 5 | INFRA-03 | `docs/deploy-cloudrun.md` documents bootstrap, deploy, rollback, logs, cost | doc | `test -f docs/deploy-cloudrun.md && grep -qE 'bootstrap\|rollback\|logs' docs/deploy-cloudrun.md` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/storage/test_journal_mode.py` — regression for PRAGMA journal_mode=DELETE + synchronous=FULL
- [ ] `tests/deploy/__init__.py` — new test package
- [ ] `tests/deploy/test_cloudrun_smoke.py` — env-guarded live URL smoke test
- [ ] `scripts/deploy-cloudrun.sh` — create (executable)
- [ ] `scripts/bootstrap-cloudrun.sh` — create (executable)
- [ ] `docs/deploy-cloudrun.md` — create
- [ ] `Dockerfile` — update CMD to shell-form honoring `${PORT:-8000}`
- [ ] `src/meshek_ml/storage/merchant_store.py` — swap journal_mode WAL → DELETE, add synchronous=FULL
- [ ] `src/meshek_ml/service/errors.py` — add `severity` field to JSONFormatter payload

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| One-time bootstrap in `meshek-prod` project | INFRA-03 | Requires operator GCP credentials | `./scripts/bootstrap-cloudrun.sh` — creates AR repo, GCS bucket, SA, IAM bindings |
| First live deploy | INFRA-03 | Requires GCP billing | `./scripts/deploy-cloudrun.sh` — builds via Cloud Build, deploys to Cloud Run, prints URL |
| Live smoke against deployed URL | INFRA-03 | Needs live URL reachable from operator machine (may require VPC reachability or a brief public-ingress window) | `MESHEK_CLOUDRUN_SMOKE=1 MESHEK_CLOUDRUN_URL=$(gcloud run services describe meshek-ml --region me-west1 --format='value(status.url)') .venv/bin/python -m pytest tests/deploy -x` |
| Rollback drill | INFRA-03 | Requires prior revision | `gcloud run services update-traffic meshek-ml --to-revisions=PREVIOUS=100 --region me-west1` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s for unit tests
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
