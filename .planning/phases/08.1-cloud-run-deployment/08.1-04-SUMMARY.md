---
phase: 08.1-cloud-run-deployment
plan: "04"
subsystem: deploy/testing
tags: [smoke-test, cloud-run, urllib, integration, hebrew-parsing]
dependency_graph:
  requires: ["08.1-01", "08.1-02", "08.1-03"]
  provides: ["live-smoke-scaffold"]
  affects: ["tests/deploy/test_cloudrun_smoke.py"]
tech_stack:
  added: []
  patterns: [env-guarded integration test, urllib.request POST helper, HTTPError catch]
key_files:
  created: []
  modified:
    - tests/deploy/test_cloudrun_smoke.py
decisions:
  - "Used AND guard (both MESHEK_CLOUDRUN_SMOKE=1 AND MESHEK_CLOUDRUN_URL) — same as D-29/D-30"
  - "HTTPError re-raised as AssertionError with response body per Phase 8 lesson (commit e3e8b12)"
  - "_TIMEOUT_S=60 to cover Cloud Run cold start + LightGBM model load"
  - "Module-level pytestmark (not per-test decorator) to apply integration mark globally"
metrics:
  duration_minutes: 5
  completed_date: "2026-04-15T12:06:21Z"
  tasks_completed: 2
  files_modified: 1
---

# Phase 8.1 Plan 04: Cloud Run Smoke Test Implementation Summary

**One-liner:** Env-guarded stdlib urllib smoke test hitting three Cloud Run endpoints (create merchant, Hebrew sales, recommend) with HTTPError catch and 60s cold-start timeout.

## What Was Built

Replaced the Wave-0 placeholder in `tests/deploy/test_cloudrun_smoke.py` with the full three-request flow replicating the Phase 8 Docker smoke pattern against a live Cloud Run URL.

### Test flow (`test_cloudrun_full_merchant_flow`)

1. `POST /merchants {}` — asserts `201 Created`, captures `merchant_id` UUID
2. `POST /merchants/{merchant_id}/sales` with `{"text": "20 עגבניות, 5 מלפפונים"}` — asserts `200 OK`, validates `parsed` is a non-empty list
3. `POST /merchants/{merchant_id}/recommend` — asserts `200 OK`, validates `recommendations` key is a list

### Guards and markers

- Both `MESHEK_CLOUDRUN_SMOKE=1` AND `MESHEK_CLOUDRUN_URL` must be set (AND, not OR)
- `pytestmark = pytest.mark.integration` at module level
- `@pytest.mark.skipif(not _SMOKE_ENABLED, ...)` on the test function
- Without env vars: silently skips — never hits a live URL in CI or normal runs

### Implementation notes

- `_post()` helper mirrors `test_docker_smoke.py` pattern exactly
- `urllib.error.HTTPError` caught, body decoded with `errors="replace"`, re-raised as `AssertionError` for debuggable output
- `_TIMEOUT_S = 60` accommodates Cloud Run cold start + LightGBM model load
- Hebrew literal `"20 עגבניות, 5 מלפפונים"` unchanged — products exist in `products_he.yaml` (commit 9ef35e5)
- Stdlib only — no new test dependencies

## Verification Results

```
.venv/bin/python -m pytest tests/deploy --collect-only -q
  1 test collected

.venv/bin/python -m pytest tests/deploy -q
  1 skipped

.venv/bin/python -m pytest tests -q
  211 passed, 2 skipped, 3 warnings (full suite)
```

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 8.1-04-01 + 8.1-04-02 | `3950934` | feat(08.1-04): implement live Cloud Run smoke test |

## Deviations from Plan

None — plan executed exactly as written. Tasks 8.1-04-01 and 8.1-04-02 were combined into a single commit per the plan's combined task definition.

## Known Stubs

None. The test scaffold is intentionally not connected to a live URL — that is the operator task in Plan 05. The scaffold is complete and correct.

## Threat Flags

None. No new network endpoints, auth paths, or schema changes introduced. The test file adds no production surface — it only issues outbound requests when the operator explicitly provides both env vars.

## Self-Check: PASSED

- `tests/deploy/test_cloudrun_smoke.py` exists and contains Hebrew literal `עגבניות`
- Commit `3950934` present in git log
- 1 test collected, 1 skipped, 0 errors in pytest collect + run
- Full suite: 211 passed, 2 skipped, 0 failures
