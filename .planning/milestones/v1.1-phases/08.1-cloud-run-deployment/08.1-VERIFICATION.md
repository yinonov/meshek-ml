---
phase: 8.1-cloud-run-deployment
verified: 2026-04-15T12:20:00Z
status: passed
must_haves_verified: 6
must_haves_total: 6
live_deploy:
  service_url: "https://meshek-ml-aiaajb5omq-zf.a.run.app"
  region: me-west1
  project: meshek-prod
  revision: meshek-ml-00001-mln
  ingress: internal-and-cloud-load-balancing
  gcs_bucket: gs://meshek-prod-merchants
---

> **Update 2026-04-15:** Live deploy executed end-to-end. Cloud Build → Artifact Registry → Cloud Run revision `meshek-ml-00001-mln`, GCS FUSE volume mounted at `/var/lib/meshek/merchants`. Verified against the live URL (`https://meshek-ml-aiaajb5omq-zf.a.run.app`) with an identity token: `GET /health` → **503** (degraded, no model bundle, valid `{status:ok,model_loaded:false,version:1.1.0}`), `POST /merchants {}` → **201**, `POST /sales` with Hebrew text `"20 עגבניות, 5 מלפפונים"` → **200** `{accepted_rows:2}`, `POST /recommend` → **200** `{reasoning_tier:"pooled_prior",confidence_score:0.3}`. GCS bucket inspection confirms two `.sqlite` files persisted via FUSE (one per merchant created during smoke). Ingress restored to `internal-and-cloud-load-balancing` after smoke. One deploy-script fix landed (`933e7d9`): Cloud Build pinned to `--region me-west1` because the `global` region is permission-restricted in `meshek-prod`. Status promoted to **passed** (6/6).


# Phase 8.1: Cloud Run Deployment — Verification Report

**Phase Goal:** The meshek-ml service runs on Google Cloud Run in the existing `meshek-prod` GCP project with per-merchant SQLite files persisted across container restarts via a GCS FUSE volume mount, deployable with a single `gcloud`/script invocation.
**Verified:** 2026-04-15
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | `scripts/deploy-cloudrun.sh` deploys to Cloud Run in meshek-prod/me-west1 via Artifact Registry, returning a live URL | VERIFIED (code) | Script exists with correct project/region defaults, gcloud builds submit + gcloud run deploy calls, DRY_RUN mode, and URL-print step. All D-06..D-24 flags present (see below). |
| SC-2 | GCS bucket mounted at `/var/lib/meshek/merchants` via native GCS FUSE volume integration with gen2 execution environment | VERIFIED (code) | `--add-volume "name=merchants-vol,type=cloud-storage,bucket=${BUCKET}"`, `--add-volume-mount "volume=merchants-vol,mount-path=${MOUNT_PATH}"`, `--execution-environment=gen2` all present in deploy script (lines 64–66). |
| SC-3 | `MerchantStore` forces SQLite `journal_mode=DELETE` so FUSE/WAL incompatibility cannot corrupt merchant databases | VERIFIED + TESTED | `merchant_store.py` lines 233–234: `PRAGMA journal_mode = DELETE` and `PRAGMA synchronous = FULL` issued before `_apply_migrations`. Both regression tests pass: `test_journal_mode_is_delete` and `test_synchronous_full` (2 passed). |
| SC-4 | `GET /health` on the deployed service URL returns 200 or 503 within 30s | HUMAN NEEDED | Requires a live Cloud Run deployment with GCP credentials. Cannot verify from codebase alone. |
| SC-5 | Repeatable deploy smoke test (`tests/deploy/test_cloudrun_smoke.py`, `MESHEK_CLOUDRUN_SMOKE=1`) posts `/merchants` and `/sales` against the live URL and asserts 201/200 | VERIFIED (scaffolding) / HUMAN NEEDED (live run) | Test is collectible (1 item collected). Without flags it skips cleanly (`1 skipped`). Test covers POST /merchants (201), POST /merchants/{id}/sales with Hebrew text (200), and POST /merchants/{id}/recommend (200). `@pytest.mark.integration` marker present. Live execution against a deployed URL requires human. |
| SC-6 | `docs/deploy-cloudrun.md` documents the one-shot deploy path, env var wiring, and rollback | VERIFIED | All required sections present: §2 One-Time Bootstrap, §3 Routine Deploy, §4 Smoke Test (with `gcloud run services proxy` wiring), §5 Rollback, §6 Tail Logs / Debug, §7 Cost Expectations, §8 Known Limitations. |

**Score:** 5/6 truths fully verified from code and automated tests; 1 truth (SC-4 live /health, SC-5 live smoke run) requires human execution with GCP credentials.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/deploy-cloudrun.sh` | Single-command Cloud Run deploy with all D-06..D-24 flags | VERIFIED | 87 lines; set -euo pipefail; DRY_RUN mode; all config flags present |
| `scripts/bootstrap-cloudrun.sh` | Idempotent one-time GCP resource setup | VERIFIED | Enables APIs, creates AR repo, GCS bucket (versioning+lifecycle), SA, IAM binding; idempotent guard for each resource |
| `src/meshek_ml/storage/merchant_store.py` | PRAGMA journal_mode=DELETE + synchronous=FULL before _apply_migrations | VERIFIED | Lines 231–235: foreign_keys ON, then journal_mode=DELETE, then synchronous=FULL, then _apply_migrations |
| `src/meshek_ml/service/errors.py` | JSONFormatter emits `severity` field for Cloud Logging | VERIFIED | Line 202: `"severity": record.levelname` emitted alongside `level` |
| `Dockerfile` | Shell-form CMD honors `${PORT:-8000}` for Cloud Run PORT injection | VERIFIED | Line 51: `CMD uvicorn meshek_ml.service.app:create_app --factory --host 0.0.0.0 --port ${PORT:-8000}` — shell-form, not exec-form |
| `tests/deploy/test_cloudrun_smoke.py` | Env-guarded integration test covering /merchants, /sales, /recommend | VERIFIED | Collectible, skips without flags, covers full 3-step flow, `@pytest.mark.integration` |
| `tests/storage/test_journal_mode.py` | Regression tests for D-10/D-11 | VERIFIED + PASSING | 2 tests: journal_mode=delete, synchronous=FULL(2) — both pass |
| `docs/deploy-cloudrun.md` | Bootstrap, deploy, rollback, logs, cost documentation | VERIFIED | 8 sections covering all required operational topics |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `deploy-cloudrun.sh` | Cloud Run gen2 | `--execution-environment=gen2` | WIRED | Line 64; required for GCS FUSE volume mounts (D-19) |
| `deploy-cloudrun.sh` | GCS FUSE volume | `--add-volume type=cloud-storage` + `--add-volume-mount` | WIRED | Lines 65–66; bucket and mount-path set to correct values |
| `deploy-cloudrun.sh` | Artifact Registry | `gcloud builds submit --tag me-west1-docker.pkg.dev/meshek-prod/meshek/...` | WIRED | Lines 54–56; image path matches D-04 |
| `deploy-cloudrun.sh` | Env vars on service | `--set-env-vars MESHEK_DATA_DIR=/var/lib/meshek/merchants,...` | WIRED | Lines 40–43, 77; four D-23 env vars set |
| `MerchantStore.__init__` | journal_mode=DELETE | PRAGMA before _apply_migrations | WIRED | Lines 233–235; correct ordering confirmed |
| `Dockerfile CMD` | `$PORT` env var | shell-form `${PORT:-8000}` | WIRED | Line 51; fallback 8000 preserves Fly.io |
| `test_cloudrun_smoke.py` | Live URL guard | `MESHEK_CLOUDRUN_SMOKE=1 AND MESHEK_CLOUDRUN_URL` | WIRED | Both conditions required at module level (lines 35–38) |

---

## D-01..D-40 Coverage Summary

All in-scope decisions verified against shipped code:

| Decision | Description | Verified |
|----------|-------------|---------|
| D-01 | Deploy into meshek-prod (not a new project) | deploy script PROJECT=meshek-prod default |
| D-02 | Region me-west1 | REGION=me-west1 default |
| D-03 | Service name meshek-ml | SERVICE=meshek-ml default |
| D-04 | Artifact Registry me-west1-docker.pkg.dev/meshek-prod/meshek | AR_REPO set, image tagged with git SHA |
| D-05 | gcloud builds submit (Cloud Build) | emit gcloud builds submit in deploy script |
| D-06 | GCS bucket gs://meshek-prod-merchants mounted at /var/lib/meshek/merchants | --add-volume + --add-volume-mount |
| D-07 | Bucket location me-west1 | bootstrap creates bucket --location=${REGION} |
| D-08 | Bucket versioning ON + 30-day lifecycle | bootstrap: --versioning + lifecycle JSON |
| D-09 | Service account roles/storage.objectUser (least privilege) | bootstrap grants role on bucket only |
| D-10 | journal_mode=DELETE before writes | merchant_store.py line 233 |
| D-11 | synchronous=FULL | merchant_store.py line 234 |
| D-12 | Regression test for journal mode | tests/storage/test_journal_mode.py |
| D-13 | CPU=1, Memory=512Mi | --cpu 1 --memory 512Mi |
| D-14 | Min instances 0 | --min-instances 0 |
| D-15 | Max instances 2 | --max-instances 2 |
| D-16 | Concurrency 40 | --concurrency 40 |
| D-17 | Timeout 60s | --timeout 60 |
| D-18 | Startup CPU boost | --cpu-boost |
| D-19 | gen2 execution environment | --execution-environment=gen2 |
| D-20 | Ingress internal-and-cloud-load-balancing | --ingress internal-and-cloud-load-balancing |
| D-21 | --no-allow-unauthenticated + SA invoker | --no-allow-unauthenticated --service-account |
| D-22 | gcloud run services proxy for smoke test | documented in docs/deploy-cloudrun.md §4 |
| D-23 | Env vars MESHEK_DATA_DIR, MODEL_PATH, LOG_LEVEL, API_HOST | --set-env-vars with all four |
| D-24 | No secrets for v1.1 | Correctly absent; noted in docs |
| D-25 | Shell-form CMD ${PORT:-8000} | Dockerfile line 51 |
| D-26 | deploy-cloudrun.sh single entry point | scripts/deploy-cloudrun.sh |
| D-27 | Idempotent, set -euo pipefail, DRY_RUN | Lines 13, 22, 45–50 |
| D-28 | bootstrap-cloudrun.sh companion script | scripts/bootstrap-cloudrun.sh |
| D-29 | test_cloudrun_smoke.py with env guard | tests/deploy/test_cloudrun_smoke.py |
| D-30 | Smoke test skipped without env flag | Confirmed: 1 skipped without flags |
| D-31 | docs/deploy-cloudrun.md | All 8 sections present |
| D-32 | Existing Phase 8 tests still pass | 211 passed, 1 skipped (Docker smoke) in full suite |
| D-33 | Docker smoke test still collectible | tests/service/test_docker_smoke.py: 1 skipped |
| D-34..D-40 | Out of scope — not implemented, not expected | Confirmed absent |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | No stubs, placeholders, or hardcoded empty data found in phase artifacts |

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| journal_mode=DELETE on fresh store | `.venv/bin/python -m pytest tests/storage/test_journal_mode.py::test_journal_mode_is_delete -x` | PASSED | PASS |
| synchronous=FULL on fresh store | `.venv/bin/python -m pytest tests/storage/test_journal_mode.py::test_synchronous_full -x` | PASSED | PASS |
| Smoke test skips cleanly without env flag | `.venv/bin/python -m pytest tests/deploy -v` | 1 skipped | PASS |
| Smoke test collectible | `.venv/bin/python -m pytest tests/deploy --collect-only` | 1 item collected | PASS |
| Full test suite | `.venv/bin/python -m pytest tests -q --ignore=tests/deploy` | 211 passed, 1 skipped | PASS |
| Live /health check | requires GCP credentials + deployed service | — | SKIP (human) |
| Live smoke test (3 endpoints) | requires MESHEK_CLOUDRUN_SMOKE=1 + deployed URL | — | SKIP (human) |

---

## Human Verification Required

### 1. GET /health on Live Deployed URL

**Test:** Run `./scripts/deploy-cloudrun.sh` (or `DRY_RUN=1` first to preview), then `curl -s $(gcloud run services describe meshek-ml --region me-west1 --format='value(status.url)')/health`
**Expected:** HTTP 200 (`{"status": "ok", "model_loaded": true}`) or HTTP 503 (`{"status": "degraded"}`) within 30 seconds of the deploy completing
**Why human:** Requires GCP project credentials, a live Cloud Run revision, and the GCS FUSE volume mount to be attached. The /health response also validates that the startup CPU boost and container cold-start complete within the timeout window.

### 2. Live Cloud Run Smoke Test (3 Endpoints)

**Test:**
```bash
# Terminal 1: open IAM proxy
gcloud run services proxy meshek-ml --region me-west1 --project meshek-prod

# Terminal 2: run smoke test
MESHEK_CLOUDRUN_SMOKE=1 \
MESHEK_CLOUDRUN_URL=http://localhost:8080 \
.venv/bin/python -m pytest tests/deploy/test_cloudrun_smoke.py -x -v
```
**Expected:** 3 assertions pass — POST /merchants returns 201 with `merchant_id`, POST /merchants/{id}/sales with Hebrew text returns 200 with non-empty `parsed` list, POST /merchants/{id}/recommend returns 200 with `recommendations` list
**Why human:** Requires a deployed Cloud Run revision with GCS FUSE mount at `/var/lib/meshek/merchants` actively attached. The test guard (`MESHEK_CLOUDRUN_SMOKE=1 AND MESHEK_CLOUDRUN_URL`) deliberately prevents offline execution. End-to-end data flow (merchant creation → Hebrew parse → recommendation) also validates the GCS FUSE persistence path.

---

## Gaps Summary

No gaps. All six success criteria are satisfied in code or require live GCP execution (which is the intended operator step). The automated scaffolding — deploy script, bootstrap script, PRAGMA fix, regression tests, and documentation — is complete and correct.

The two human verification items are inherent to the nature of a cloud deployment verification; they are not implementation gaps.

---

## VERIFICATION COMPLETE

_Verified: 2026-04-15_
_Verifier: Claude (gsd-verifier)_
