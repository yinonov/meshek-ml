---
phase: 07-hebrew-input-parsing
plan: 04
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - src/meshek_ml/parsing/__init__.py
  - tests/parsing/test_integration.py
autonomous: true
requirements: [PARSE-01, PARSE-02]
must_haves:
  truths:
    - "meshek_ml.parsing exports ParsedSale, ParseError, parse_sales_line, parse_sales_lines, ProductCatalog, load_catalog, Unit, DEFAULT_CATALOG_PATH"
    - "Running against the real configs/parsing/products_he.yaml catalog, all six D-18 test cases pass"
    - "Case (a) singular/plural/common-misspelling of a seed product all resolve to the same canonical id"
    - "Case (b) 'NUMBER PRODUCT' and 'PRODUCT NUMBER UNIT' both parse into matching ParsedSale"
    - "Case (c) an obviously non-product Hebrew word returns ParseError(kind='unknown_product')"
    - "Case (d) a niqqud-bearing input parses identically to its niqqud-free twin"
    - "Case (e) קג / ק״ג / קילו all produce Unit.KG on a real seed product"
    - "Case (f) empty and whitespace-only inputs return ParseError(kind='empty_input')"
  artifacts:
    - path: src/meshek_ml/parsing/__init__.py
      provides: "Public package API"
      exports: ["ParsedSale", "ParseError", "parse_sales_line", "parse_sales_lines", "ProductCatalog", "Product", "load_catalog", "Unit", "DEFAULT_CATALOG_PATH", "CatalogError"]
    - path: tests/parsing/test_integration.py
      provides: "End-to-end tests against the real seed catalog covering D-18 cases a–f"
  key_links:
    - from: src/meshek_ml/parsing/__init__.py
      to: parser, catalog, normalize submodules
      via: "re-exports from .parser, .catalog, .normalize"
      pattern: "from meshek_ml.parsing"
---

<objective>
Wave 4: package the public API and prove the six D-18 integration cases against the real seed YAML. This is the phase-completion gate — it is the only plan that loads the shipped `configs/parsing/products_he.yaml` in a test.

Purpose: `__init__.py` collapses the three-module surface into a single import for Phase 8 (`from meshek_ml.parsing import parse_sales_line, load_catalog, DEFAULT_CATALOG_PATH`). The integration tests verify the library end-to-end against real data — this is what closes PARSE-01 and PARSE-02.
Output: Public `__init__.py`, integration test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-hebrew-input-parsing/07-CONTEXT.md
@.planning/phases/07-hebrew-input-parsing/07-01-PLAN.md
@.planning/phases/07-hebrew-input-parsing/07-02-PLAN.md
@.planning/phases/07-hebrew-input-parsing/07-03-PLAN.md
@src/meshek_ml/parsing/normalize.py
@src/meshek_ml/parsing/catalog.py
@src/meshek_ml/parsing/parser.py
@configs/parsing/products_he.yaml

<interfaces>
Final public API surface:

```python
# src/meshek_ml/parsing/__init__.py
from meshek_ml.parsing.normalize import Unit, normalize_text
from meshek_ml.parsing.catalog import (
    Product,
    ProductCatalog,
    CatalogError,
    load_catalog,
    DEFAULT_CATALOG_PATH,
)
from meshek_ml.parsing.parser import (
    ParsedSale,
    ParseError,
    parse_sales_line,
    parse_sales_lines,
)

__all__ = [
    "Unit",
    "normalize_text",
    "Product",
    "ProductCatalog",
    "CatalogError",
    "load_catalog",
    "DEFAULT_CATALOG_PATH",
    "ParsedSale",
    "ParseError",
    "parse_sales_line",
    "parse_sales_lines",
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export public package API and land D-18 integration tests against real seed catalog</name>
  <files>src/meshek_ml/parsing/__init__.py, tests/parsing/test_integration.py</files>
  <read_first>
    - configs/parsing/products_he.yaml (to pick a seed product with a known niqqud-friendly Hebrew form and a common misspelling — tomato is the canonical choice)
    - src/meshek_ml/parsing/parser.py
    - src/meshek_ml/parsing/catalog.py
    - .planning/phases/07-hebrew-input-parsing/07-CONTEXT.md §Testing Contract (D-18)
  </read_first>
  <behavior>
    All tests use a module-scoped fixture `seed_catalog` that calls `load_catalog(DEFAULT_CATALOG_PATH)`.

    - test_integration.py::test_singular_plural_misspelling_same_id — for "tomato" (or whichever seed product shipped with singular+plural+misspelling aliases): each of the three variants passed as a full line `"5 {variant}"` produces ParsedSale with the SAME product_id.
    - test_integration.py::test_number_before_and_after_order_invariant — `"20 עגבניות"` and `"עגבניות 20 קג"` both return ParsedSale with product_id=="tomato" and quantity==20.0.
    - test_integration.py::test_unknown_product_returns_structured_error — a clearly non-seed Hebrew word (e.g. `"5 זנב"` — "tail") returns `ParseError(kind="unknown_product")`.
    - test_integration.py::test_niqqud_input_parses_identically — a niqqud-bearing version of a seed alias (constructed inline, e.g. `"5 עֲגָבְנִיָּה"`) returns the SAME product_id as the plain version `"5 עגבניה"`.
    - test_integration.py::test_kg_unit_variants_all_map_to_kg — `"5 קג עגבניות"`, `"5 ק״ג עגבניות"`, `"5 קילו עגבניות"` all return ParsedSale with `unit == Unit.KG`.
    - test_integration.py::test_empty_and_whitespace — `parse_sales_line("", cat)` and `parse_sales_line("   ", cat)` both return `ParseError(kind="empty_input")`.
    - test_integration.py::test_public_api_importable — assert that `parse_sales_line`, `load_catalog`, `DEFAULT_CATALOG_PATH`, `ParsedSale`, `ParseError`, `Unit`, `ProductCatalog` are all importable from `meshek_ml.parsing` directly (not through submodules).

    Guard: before running the identity-based tests, the fixture asserts that the chosen seed product (`tomato`) actually exists in the loaded catalog; if not, fail with a clear message so future catalog edits don't silently break the test.
  </behavior>
  <action>
    1. Create `src/meshek_ml/parsing/__init__.py` EXACTLY as the <interfaces> block above. Replace the Plan 01 stub. Keep `from __future__ import annotations` at the top.
    2. Create `tests/parsing/test_integration.py`:
       - Module-scoped fixture `seed_catalog()` → `load_catalog(DEFAULT_CATALOG_PATH)`.
       - Guard fixture verifies `"tomato"` is a known product_id (fail fast otherwise).
       - Implement every behavior listed above.
       - For the niqqud test, embed the niqqud-bearing string directly as a literal in the source file (UTF-8).
       - The kg-variants test must assert `unit == Unit.KG` on all three variants and `product_id` matches.
       - The public-API test uses `from meshek_ml.parsing import parse_sales_line, load_catalog, DEFAULT_CATALOG_PATH, ParsedSale, ParseError, Unit, ProductCatalog` and then `assert callable(parse_sales_line)`.
    3. Run `pytest tests/parsing -q --no-cov` — all four test files green.
    4. Run a smoke script covering a realistic mixed batch:
       ```
       python -c "from meshek_ml.parsing import parse_sales_lines, load_catalog, DEFAULT_CATALOG_PATH, ParsedSale, ParseError; c=load_catalog(DEFAULT_CATALOG_PATH); r=parse_sales_lines(['20 עגבניות','עגבניות 20 קג','','10 זנב'], c); assert isinstance(r[0], ParsedSale) and isinstance(r[1], ParsedSale) and isinstance(r[2], ParseError) and isinstance(r[3], ParseError); print('ok', [type(x).__name__ for x in r])"
       ```
    5. Run the full `pytest -q --no-cov` suite to confirm no regression in other packages (forecasting / storage / recommendation / service).
  </action>
  <verify>
    <automated>pytest tests/parsing -q --no-cov && python -c "from meshek_ml.parsing import parse_sales_line, parse_sales_lines, load_catalog, DEFAULT_CATALOG_PATH, ParsedSale, ParseError, Unit, ProductCatalog; c=load_catalog(DEFAULT_CATALOG_PATH); r=parse_sales_line('20 עגבניות', c); assert isinstance(r, ParsedSale); assert r.quantity == 20.0" && pytest -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '__all__' src/meshek_ml/parsing/__init__.py`
    - `grep -q 'parse_sales_line' src/meshek_ml/parsing/__init__.py`
    - `grep -q 'load_catalog' src/meshek_ml/parsing/__init__.py`
    - `grep -q 'DEFAULT_CATALOG_PATH' src/meshek_ml/parsing/__init__.py`
    - `python -c "from meshek_ml.parsing import parse_sales_line, load_catalog, DEFAULT_CATALOG_PATH, ParsedSale, ParseError, Unit, ProductCatalog"` exits 0
    - `pytest tests/parsing -q --no-cov` exits 0
    - `pytest -q --no-cov` exits 0 (no regression in Phases 5/6 suites)
  </acceptance_criteria>
  <done>Phase 7 closed. PARSE-01 and PARSE-02 verified against the real seed catalog. Phase 8 can import `parse_sales_line` directly from `meshek_ml.parsing`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public package import surface | Consumed by Phase 8 FastAPI route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-10 | Tampering | Seed YAML drift breaking identity tests | mitigate | Integration fixture asserts `"tomato"` exists; fails fast on catalog edits that remove it. |
| T-7-11 | Repudiation | ParseError lacks provenance | accept | raw_text is preserved on ParseError; Phase 8 will log it. |
</threat_model>

<verification>
Full repo test suite green: `pytest -q --no-cov`.
`tests/parsing/test_integration.py` covers every D-18 case.
</verification>

<success_criteria>
- Every ROADMAP Phase 7 success criterion has a passing test in `test_integration.py`.
- `meshek_ml.parsing` is a single clean import surface.
- No regression in Phase 5 / Phase 6 suites.
</success_criteria>

<output>
After completion, create `.planning/phases/07-hebrew-input-parsing/07-04-SUMMARY.md` and mark Phase 7 complete in ROADMAP.md.

Commit template:
```
feat(07-04): expose meshek_ml.parsing public API and D-18 integration tests

Closes PARSE-01 and PARSE-02. Integration suite verifies singular/plural/
misspelling identity, order-invariant quantity extraction, niqqud parity,
kg-unit variants, and empty/unknown error kinds against the real seed
catalog.
```
</output>
