---
phase: 08-api-surface-deployment
plan: 06
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05]
files_modified:
  - Dockerfile
  - .dockerignore
  - fly.toml
  - tests/service/test_docker_smoke.py
autonomous: true
requirements: [INFRA-02]
must_haves:
  truths:
    - "docker build -t meshek-ml:smoke . produces an image using only service + runtime extras"
    - "docker run -p 18000:8000 -e MESHEK_DATA_DIR=/tmp/merchants meshek-ml:smoke responds 200 or 503 on GET /health within 15 seconds"
    - "fly.toml declares internal_port 8000, http_checks /health, 512MB VM, persistent mount for MESHEK_DATA_DIR"
    - "Image does NOT contain torch, stable-baselines3, flwr, streamlit, or u8darts"
  artifacts:
    - path: "Dockerfile"
      provides: "single-stage python:3.12-slim image with uv sync --locked --no-dev --extra service --extra runtime"
    - path: ".dockerignore"
      provides: "excludes .venv, notebooks/, reports/, outputs/, data/, .planning/, academic/, tests/, __pycache__"
    - path: "fly.toml"
      provides: "primary_region=ams, http_service internal_port 8000, [[http_service.checks]] path=/health, [[vm]] 512mb, [[mounts]] for persistent data"
    - path: "tests/service/test_docker_smoke.py"
      provides: "env-guarded integration test that builds + runs the image + curls /health"
  key_links:
    - from: "Dockerfile CMD"
      to: "uvicorn --factory meshek_ml.service.app:create_app"
      via: ".venv/bin/uvicorn"
      pattern: "uvicorn.*--factory"
    - from: "fly.toml [build]"
      to: "Dockerfile"
      via: "dockerfile = \"Dockerfile\""
      pattern: "dockerfile = \"Dockerfile\""
---

<objective>
Implement INFRA-02: ship a Docker image that runs the Phase 8 service on Fly.io (primary) and Railway (fallback), plus an env-guarded smoke test that proves the image builds and `/health` responds inside the container.

Output: `Dockerfile`, `.dockerignore`, `fly.toml`, `tests/service/test_docker_smoke.py`, and a one-paragraph "Deploy to Railway" section in the phase summary template (written in 08-06-SUMMARY.md on completion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-api-surface-deployment/08-CONTEXT.md
@.planning/phases/08-api-surface-deployment/08-RESEARCH.md
@.planning/phases/08-api-surface-deployment/08-VALIDATION.md
@pyproject.toml
@uv.lock
</context>

Research anchors:
- §Docker with uv — verified Dockerfile recipe (single-stage, `uv sync --locked --no-dev --extra service --extra runtime`, non-root `appuser`, `.venv/bin/uvicorn --factory`, HEALTHCHECK via urllib).
- §fly.toml — verified config including `[[http_service.checks]]`, `[[vm]]`, `[[mounts]]`.
- §Common Pitfall 8 — `uv.lock` must be up-to-date (plan 01's `uv lock` run satisfies this).
- §Common Pitfall 3 — `MESHEK_DATA_DIR` must be set before the service starts; Dockerfile sets it via ENV and creates the directory with `appuser` ownership.

<tasks>

<task type="auto">
  <name>Task 8-06-01: Dockerfile + .dockerignore + fly.toml</name>
  <files>
    Dockerfile,
    .dockerignore,
    fly.toml
  </files>
  <action>
  1. `Dockerfile` — single-stage image per research §Docker with uv:
     - `FROM python:3.12-slim`
     - `COPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /bin/`
     - `WORKDIR /app`
     - `COPY pyproject.toml uv.lock ./`
     - `RUN uv sync --locked --no-dev --extra service --extra runtime`
     - `COPY src/ ./src/`
     - `COPY configs/ ./configs/`
     - `COPY models/ ./models/` (D-17; a placeholder bundle must exist at `models/lightgbm_v1.bundle` per D-19 — if the file is absent at build time, the build still succeeds because the directory copy is best-effort; the degraded-start contract from plan 01 handles the missing-model case at runtime)
     - `RUN mkdir -p /var/lib/meshek/merchants && adduser --system --no-create-home appuser && chown -R appuser /var/lib/meshek/merchants /app`
     - `USER appuser`
     - `EXPOSE 8000`
     - `ENV MESHEK_DATA_DIR=/var/lib/meshek/merchants \`
       `    MESHEK_MODEL_PATH=/app/models/lightgbm_v1.bundle \`
       `    MESHEK_MODELS_DIR=/app/models \`
       `    MESHEK_API_HOST=0.0.0.0 \`
       `    MESHEK_API_PORT=8000 \`
       `    MESHEK_LOG_LEVEL=info \`
       `    PATH="/app/.venv/bin:$PATH"`
     - `HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \ CMD python -c "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status in (200,503) else 1)" || exit 1`
       (Accept 503 as "alive" for HEALTHCHECK purposes — degraded service is still responding. Fly.io's `[[http_service.checks]]` may tighten this to 200-only; that's a Fly.io-side concern.)
     - `CMD ["uvicorn", "meshek_ml.service.app:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]`
  2. `.dockerignore` (D-20) — exclude:
     ```
     .venv
     .git
     .gitignore
     .planning
     notebooks
     reports
     outputs
     data
     academic
     tests
     **/__pycache__
     **/*.pyc
     .pytest_cache
     .ruff_cache
     .DS_Store
     *.egg-info
     ```
  3. `fly.toml` — verbatim from research §fly.toml, with:
     - `app = "meshek-ml"`
     - `primary_region = "ams"` (Amsterdam; closest Fly.io region to Israel as of 2026)
     - `[build] dockerfile = "Dockerfile"`
     - `[env]` block with `MESHEK_API_HOST`, `MESHEK_API_PORT`, `MESHEK_LOG_LEVEL`
     - `[http_service] internal_port = 8000, force_https = true, auto_stop_machines = "stop", auto_start_machines = true, min_machines_running = 0`
     - `[[http_service.checks]] path = "/health", method = "GET", interval = "30s", timeout = "5s", grace_period = "15s"` — intentionally stricter than the Dockerfile HEALTHCHECK: Fly.io expects 200, so a degraded 503 will cause Fly to reroute, which is the intended operator signal.
     - `[[vm]] memory = "512mb", cpus = 1`
     - `[[mounts]] source = "meshek_data", destination = "/var/lib/meshek/merchants", initial_size = "1gb"`
  Do NOT add a `railway.json` — D-21 says Railway works with the same Dockerfile unchanged; document in the phase SUMMARY instead.
  </action>
  <verify>
    <automated>MESHEK_DOCKER_SMOKE=1 uv run pytest tests/service/test_docker_smoke.py -x</automated>
  </verify>
  <done>
  - `docker build -t meshek-ml:smoke .` exits 0 locally
  - `fly.toml` parses (`flyctl config validate` if flyctl is installed; skip otherwise — manual verification row in 08-VALIDATION.md)
  - `.dockerignore` excludes every forbidden path
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 8-06-02: env-guarded Docker smoke test</name>
  <files>
    tests/service/test_docker_smoke.py
  </files>
  <behavior>
  - `test_health`: when `MESHEK_DOCKER_SMOKE=1`, build the image, run it with `-p 18000:8000 -e MESHEK_DATA_DIR=/tmp/merchants`, poll `http://localhost:18000/health` for up to 15 seconds, assert status is 200 OR 503 (503 is acceptable because the image may not have a valid model bundle baked in — the degraded-start contract applies). Clean up the container in a `finally` block.
  - When `MESHEK_DOCKER_SMOKE` is unset, the test is skipped via `pytest.mark.skipif`.
  - Marked `@pytest.mark.integration` so `-m "not integration"` excludes it from the default run.
  </behavior>
  <action>
  Implement per research §Docker Smoke Test Pattern. Use `subprocess.run(["docker","build","-t","meshek-ml-smoke:test","."], check=True, timeout=600)` for the build (allow 10 minutes for cold Docker cache). Poll `/health` with a 1-second backoff loop up to 15 attempts. Assert `response.status in (200, 503)`. Tear down with `docker rm -f` in `finally`.
  Replace the Wave 0 stub file from plan 01 with the real implementation.
  </action>
  <verify>
    <automated>MESHEK_DOCKER_SMOKE=1 uv run pytest tests/service/test_docker_smoke.py::test_health -x</automated>
  </verify>
  <done>
  - With `MESHEK_DOCKER_SMOKE=1`, the test builds, runs, and verifies the container's /health endpoint
  - Without the env flag, the test is skipped (normal `pytest tests/service` runs stay fast)
  - Container is always cleaned up, even on assertion failure
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build context → image | `.dockerignore` excludes `.planning/`, `data/`, `outputs/`, `academic/` so planning notes and raw data never ship in the image |
| container user → host filesystem | Non-root `appuser`; persistent mount is the only writable path outside `/app` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-14 | Information Disclosure | Planning notes / raw merchant data leaking into the published image | mitigate | `.dockerignore` excludes `.planning/`, `data/`, `outputs/`, `academic/`, `notebooks/`, `reports/`, `tests/` (D-20). Verified by grepping the image for any of these paths: `docker run --rm meshek-ml:smoke find /app -name "*.planning*"` returns nothing. |
| T-8-15 | Elevation of Privilege | Container runs as root, hostile request escapes to host | mitigate | `USER appuser` (non-root, no home dir). D-17. |
| T-8-16 | Tampering | Dependency drift between `uv.lock` and `pyproject.toml` produces a different image than local `uv sync` | mitigate | `uv sync --locked` asserts lock consistency; pitfall 8. Build fails fast if `uv.lock` is stale. |
| T-8-17 | Availability | Missing model bundle → container crash loop | accept (mitigated by plan 01) | Degraded-start contract in `create_app()` lets the container boot; /health returns 503; Fly.io `[[http_service.checks]]` reroutes. Operator sees the 503 in the Fly.io dashboard. |
| T-8-18 | Information Disclosure | Secrets baked into the image via ENV | accept | No secrets are passed via ENV — only non-sensitive config (paths, host, port, log level). `MESHEK_MODEL_PATH` is an operator-controlled path, not a secret; inherited from Phase 6 T-6-12. |
</threat_model>

<integration_notes>
- **Railway fallback (D-21):** the same Dockerfile works on Railway unchanged — Railway auto-detects `EXPOSE 8000`, honors the `HEALTHCHECK`, and routes traffic. Set `MESHEK_DATA_DIR`, `MESHEK_MODEL_PATH` via Railway's dashboard env UI. No `railway.json` required. This will be documented as a paragraph in `08-06-SUMMARY.md`:
  > **Deploy to Railway (fallback):** `railway link` the repo in the Railway dashboard; Railway will detect `Dockerfile` automatically. Set `MESHEK_DATA_DIR=/var/lib/meshek/merchants`, `MESHEK_MODEL_PATH=/app/models/lightgbm_v1.bundle`, `MESHEK_LOG_LEVEL=info` in the service's Variables tab. Add a persistent volume mounted at `/var/lib/meshek/merchants`. Deploy. Confirm `GET https://<service>.up.railway.app/health` returns 200 once a model bundle is uploaded.
- **HEALTHCHECK vs Fly.io checks:** deliberately asymmetric — Dockerfile HEALTHCHECK accepts 503 (container is alive and responding), Fly.io check requires 200 (service is fully healthy). This gives operators two signals: Docker sees a live container, Fly.io reroutes away from a degraded one.
- **Manual-only verifications from 08-VALIDATION.md:** two rows require external accounts (Fly.io deploy, Railway deploy). These are captured in the phase verification doc and are not part of this plan's automated gates.
</integration_notes>

<rollback>
Delete `Dockerfile`, `.dockerignore`, `fly.toml`, and revert `tests/service/test_docker_smoke.py` to its Wave 0 stub.
</rollback>

<verification>
- `docker build -t meshek-ml:smoke .` exits 0 (run before the pytest gate)
- `MESHEK_DOCKER_SMOKE=1 uv run pytest tests/service/test_docker_smoke.py -x` passes
- `uv run pytest tests/service -x -m "not integration"` still green (smoke test skipped without the env flag)
- Image size sanity check: `docker image inspect meshek-ml:smoke --format '{{.Size}}'` is < 1.5 GB
- Image contents sanity check: `docker run --rm meshek-ml:smoke python -c "import sys; import importlib.util; assert importlib.util.find_spec('torch') is None and importlib.util.find_spec('streamlit') is None and importlib.util.find_spec('flwr') is None"` exits 0
</verification>

<success_criteria>
- INFRA-02 satisfied: the service runs inside a Docker container with /health responding
- `Dockerfile`, `.dockerignore`, `fly.toml` in repo root, committed
- Env-guarded smoke test exists and passes when enabled
- Railway fallback documented in 08-06-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/08-api-surface-deployment/08-06-SUMMARY.md` including the one-paragraph "Deploy to Railway" section.
</output>
